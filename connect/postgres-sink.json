{
    "name": "postgres-sink",
    "config": {
        "auto.create": "true",
        "connection.password": "example",
        "connection.url": "jdbc:postgresql://db:5432/test",
        "connection.user": "postgres",
        "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
        "insert.mode": "upsert",
        "name": "postgres-sink",
        "pk.fields": "id",
        "pk.mode": "record_key",
        "tasks.max": "1",
        "topics": "lobby-events, corporate-events"
    }
}